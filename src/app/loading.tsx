//este es el componente que se carga cuando se estan cargando los valores del server
//se muestra mientras se estan cargando las paginas dinamicas como [id] en este caso
export default function LoadingPage() {
  return <span className="loading loading-dots loading-lg m-auto block" />;
}
